rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Profile photos live at users/{uid}/profile.jpg — owner-only access.
    match /users/{userId}/{file=**} {
      allow read, write: if request.auth != null
                         && request.auth.uid == userId;
    }
  }
}
